import {calculatePokemon} from './Function/calculatePokemon';
import react from 'react';

const debug = () => {
    
    const listy = [{'a': 'b'}, {'c': 'd'}]

    return ({
        <div>
            adf
        </div>
    });

}

export default debug